steps:
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - run
      - deploy
      - google-function-app
      - --source=.
      - --region=us-central1
      - --platform=managed
      - --set-env-vars=OTEL_SERVICE_NAME=google-function-app,GCP_PROJECT_ID=$PROJECT_ID
      - --project=$PROJECT_ID

options:
  logging: CLOUD_LOGGING_ONLY
